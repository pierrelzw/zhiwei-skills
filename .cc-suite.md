# CC-Suite Configuration

Project-specific settings for cc-suite commands.
Generated by `/init`. Edit freely — all fields are optional.

## Project

- **Stack**: JSON + Markdown (Claude Code plugin marketplace registry)
- **Test command**: (none)
- **Source directories**: .claude-plugin/

## Defaults

These override the built-in defaults for all commands in this project.
Remove a line to fall back to the built-in default.

- **Default model**: gpt-5.5
- **Default effort**: high
- **Default audit type**: full
- **Default sandbox**: workspace-write

## Audit Focus

balanced

Additional instructions appended to every audit's developer-instructions:

```text
Give equal attention to all audit dimensions.
```

## Skip Patterns

Files and directories to always skip during audits (glob patterns):

```text
node_modules/
dist/
build/
coverage/
*.min.js
*.bundle.js
*.lock
vendor/
.git/
.research/
.review-board/
.gstack/
```

## Project-Specific Instructions

Custom instructions appended to Codex's developer-instructions for every command.
Use this to tell Codex about your project's conventions, architecture, or constraints.

```text
This is a Claude Code plugin marketplace registry. The only file the marketplace
system reads is .claude-plugin/marketplace.json; README.md mirrors its plugin
table for humans. Keep the marketplace.json `version` fields in sync with each
plugin repo's .claude-plugin/plugin.json. Most "code" here is JSON config and
Markdown docs — prioritize registry correctness, valid JSON, and doc/table
accuracy over traditional code-quality heuristics.
```
